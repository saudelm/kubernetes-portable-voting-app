project_id = "my-gcp-project-id"
location   = "europe-west3-a"
image_tag  = "0123456789abcdef0123456789abcdef01234567"
image_repository_base = "ghcr.io/my-github-user/my-repository"

# Keine Kennwoerter in diese Datei schreiben. Fuer das Experiment verwenden:
# export TF_VAR_postgres_password="$(openssl rand -base64 24)"
# export TF_VAR_grafana_admin_password="$(openssl rand -base64 24)"
